@{ Database.Out oout = new(); 
 //asp-action="@oout.CallLogout(ViewBag.Id)"
}

<div class="modal" id="ModalSair">
    <div class="modal-dialog">
        <div class="modal-content m-1 border border-2 border-primary">
            <div class="card-body m-3 text-center">
                <h3 id="idCardSair" class="card-title pt-2" aria-describedby="idSubSair">Logout</h3>
                <small id="idSubSair" class="pt-2 text-muted">Deseja realmente sair de sua conta?</small>
                <div class="card-body d-flex justify-content-center pt-4">
                    <a class="btn btn-primary me-5 col-4" >Sim, desejo sair</a>
                    <button type="button" class="ms-3 btn btn-secondary col-4" data-bs-dismiss="modal">Cancelar</button>
                </div>
            </div>
        </div>
    </div>
</div>